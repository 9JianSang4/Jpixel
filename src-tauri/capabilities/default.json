{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "pin-*", "editor-*", "capture-*"],
  "permissions": [
    "core:default",
    "opener:default",
    "global-shortcut:default",
    "dialog:default",
    "fs:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-size"
  ]
}
